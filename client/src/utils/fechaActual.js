  const fecha = new Date();
  const hora = fecha.getHours();
  const minutes = fecha.getMinutes();
  export const fechaActual = fecha.getFullYear() + '-' + (fecha.getMonth() + 1) + '-' + fecha.getDate();